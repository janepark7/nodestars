/* Stars Assignment */

// insert a for loop
/*
function draw(howMany, char) {
	var lines = howMany;
  /* Build a string of howMany chars */
//      var str = (new Array(howMany)).fill(char || '%').join("");
  // Since some browsers don't support Array.fill

	//set timeout function with the stars project

		/*var textValue = 10;
			delayLoop(textValue);

		function delayLoop(to, at) {
		    var at = at || 10;
		    if (at >= to) {
		        setTimeout(function() {
		            console.log(at);
		            delayLoop(to, at - 1);
		        }, 1000 * at);
		    }
		}
*/
// setTimeout(function(){
// 	console.log("hi");

var x,y,chr;
for(x=10; x =>1; x++)
{
   for (y=1; y > x; y++)
     {
    chr=chr-("*");
      }
 console.log(chr);
 chr='';
}
